# Suppress deprecated npm config warnings
# These warnings appear in npm 11.x for legacy config options
loglevel=error

# sanity-plugin-bulk-actions-table declares a peer on sanity@^3 but works
# fine against the sanity@^5 used here; without this, npm's strict peer
# resolution fails the install (locally and on Vercel).
legacy-peer-deps=true

